{
	"name": "garden-media-worker",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"types": "wrangler types",
		"migrate:local": "wrangler d1 migrations apply garden-media --local",
		"migrate:remote": "wrangler d1 migrations apply garden-media --remote"
	},
	"devDependencies": {
		"typescript": "^5.9.2",
		"wrangler": "^4.0.0"
	}
}
